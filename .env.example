# Neon PostgreSQL (connection string from https://neon.tech)
DATABASE_URL=postgresql://neondb_owner:npg_Vx25EKrhzqRp@ep-solitary-poetry-a1t2766f-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# API server (Elysia)
PORT=3001
WEB_ORIGIN=http://localhost:3000
INTERNAL_API_URL=http://localhost:3001

# Better Auth — use a long random string in production (e.g. openssl rand -base64 32)
BETTER_AUTH_SECRET=change-me-in-production
# Public URL where the browser loads the Next app (cookies + auth callbacks)
BETTER_AUTH_URL=http://localhost:3000

# Next.js (browser + server-side fetch to same origin)
NEXT_PUBLIC_APP_URL=http://localhost:3000
