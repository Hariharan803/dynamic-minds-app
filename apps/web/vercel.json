{
  "framework": "nextjs",
  "installCommand": "cd ../.. && curl -fsSL https://bun.sh/install | bash && export BUN_INSTALL=$HOME/.bun && export PATH=$BUN_INSTALL/bin:$PATH && bun install",
  "buildCommand": "cd ../.. && export BUN_INSTALL=$HOME/.bun && export PATH=$BUN_INSTALL/bin:$PATH && bun run --filter @student-platform/web build"
}
