{
  "name": "@student-platform/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "generate": "drizzle-kit generate --config drizzle.config.cjs",
    "push": "drizzle-kit push --config drizzle.config.cjs",
    "migrate": "drizzle-kit migrate --config drizzle.config.cjs",
    "studio": "drizzle-kit studio --config drizzle.config.cjs",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1",
    "typescript": "^5.7.2"
  }
}
